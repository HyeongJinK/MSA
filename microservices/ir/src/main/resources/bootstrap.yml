app.config-server: localhost

jhipster:
  registry:
    password: admin

spring:
  application.name: ir
  cloud.config:
    failFast: true
    retry:
      initialInterval: 3000
      multiplier: 1.3
      maxInterval: 10000
      maxAttempts: 20
    uri: http://${app.config-server}:8888

---
spring.profiles: docker

spring:
  cloud.config:
    uri: http://admin:${jhipster.registry.password}@${app.config-server}:8761/config

app.config-server: jhipster-registry

---
spring.profiles: test

spring:
  cloud.config:
    uri: http://admin:${jhipster.registry.password}@${app.config-server}:8761/config

app.config-server: jhipster-registry
---
spring.profiles: swam

app.config-server: 192.168.234.161

spring:
  cloud.config:
    uri: http://admin:${jhipster.registry.password}@${app.config-server}:8761/config

---
spring.profiles: swam-test

app.config-server: 13.125.122.99

spring:
  cloud.config:
    uri: http://admin:${jhipster.registry.password}@${app.config-server}:8761/config
---
spring.profiles: swam-new-test

app.config-server: 13.209.48.160

spring:
  cloud.config:
    uri: http://admin:${jhipster.registry.password}@${app.config-server}:8761/config